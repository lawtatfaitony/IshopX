import { NavManuToggleDirective } from './nav-manu-toggle.directive';

describe('NavManuToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavManuToggleDirective();
    expect(directive).toBeTruthy();
  });
});
